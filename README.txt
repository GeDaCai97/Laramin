Para inicializar, ejecutar 'npm i', 'composer i'.
Correr el back 'php -S localhost:8000 -t public'
Para correr el servicio de Vite ejecuta 'npm run dev'

El archivo "public/index.php" es donde se declaran las rutas.

De momento el archivo .env se debe crear manualmente en la raíz del proyecto (Tomar de ejemplo el .env.example)


/* Futuras actualizaciones */

Hacer test de directivas @csfr, @PUT, @PATCH, @DELETE, @method(), @auth, @guest

Investigar los JWT

Extender la clase de servicios de Email.

Aumentar más las relaciones en el ORM basado en Laravel (ej. BelongsTo, belongsToMany, hasOne, hasMany)

Adaptar la paginacion similar a Laravel para simplificar el codigo de paginacion

Implementar un sistema de guardado de archivos simple en código.